# Application Ports
FRONTEND_PORT=3000
BACKEND_PORT=4000
DYNAMODB_PORT=8000
LOCALSTACK_PORT=4566

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Service Endpoints
DYNAMODB_ENDPOINT=http://dynamodb-local:8000
S3_ENDPOINT=http://localstack:4566
LAMBDA_ENDPOINT=http://localstack:4566
APIGATEWAY_ENDPOINT=http://localstack:4566

# Frontend Configuration
VITE_API_URL=http://localhost:4000

# LocalStack Configuration
LOCALSTACK_SERVICES=s3,lambda,apigateway,dynamodb
LAMBDA_EXECUTOR=docker
LAMBDA_REMOTE_DOCKER=false
LAMBDA_DOCKER_NETWORK=localstack_default
LAMBDA_DOCKER_HOST=host.docker.internal 